# 项目状态快照 2026-09-08

> 与 README「功能与验证状态」表配套的进度存档。每轮重大进展追加一节，不改旧节（历史可溯）。

## 2026-09-08 晚

### 设备状态
- 固件 v12（cron 全自动简报版）；板子断电重启后已恢复：ai_agent PID 182 常驻、ssh 通道正常、mediad 存活
- IP 192.168.137.197；SSH 密钥 /data/k+/data/a 断电不丢
- cron.json：morning-briefing（at_epoch=1788912000 = **09-09 08:00 CST**，enabled）已武装为明早观察点

### 本轮完成
1. **断电容灾恢复流程验证**（重启→date/wifi/sshd/ai_agent 全链路 10 分钟内，密钥/配置/skills/cron 均在 /data 保留）
2. **cron 过期任务机制定论**（源码 + 实机双证）：at_epoch 已过的任务在实例装载时被静默禁用（enabled=false、next_run=0 并写盘），**绝不补触发**；cron.json 无热加载，新任务需重启实例装载
3. **全自动简报链路复验（自测 5/5 绿）**：cron-smoke-test 任务 20:56:54 准点 fire → inbound → MiMo LLM（read_file skill / cron_list 工具调用）→ 514 字简报 → delete_after_run 自清理 → 零崩溃
4. 语音/显示功能专项启动：语音=验证 PCM→mediad→alsasink 喇叭通路（零代码）；显示=简报上屏方案已定稿（POSIX mqueue "briefing" 队列 + luncher_mini 右上角卡片 + "简报"关键词过滤），进入实施

### 待办/未完成（如实）
- 简报上屏：代码实施中（预计 09-08~09 完成，需重编烧录）
- TTS：音频栈已修复（v11），云端 TTS 引擎（火山方舟）未接入——无账号密钥；本地通路验证中
- 屏幕简报内容只显示摘要（≤200B），全文在 CLI/session 文件
- 明早 09-09 08:00 全自动简报为下一个天然观察点（预期不崩 + 简报上屏联动）

### 证据索引（可核实）
- 自测触发时间线：`dmesg`（20:56:54 Cron job firing → Processing message → LLM resp → Agent 回复），会话存板端 `/data/ai_agent/sessions/tg_console.jsonl`
- 断电容灾与恢复全程：logs/xiaoduss/2026-09-08/

## 2026-09-07 深夜（历史）

- v12 全自动简报 4 次全绿（21:55/22:01/22:12/22:20），含真实天气（北京 17.3°C 小雨，Tavily 检索）
- 崩溃疑云收敛：skill 文件实例启动后变更 + 首次消息刷新 = 历史 2/2 崩（SPI NAND 写风暴）；无变化 = 4/4 稳——机制指向 skill refresh 写盘路径，待专项复现（已延后）

## 2026-09-06~07（历史）

- v12（cron_service.c 提醒任务 push_outbound→push_inbound）编译烧录完成；cron→inbound→agent→LLM 链路实测打通
- MiMo/Tavily 配置持久化到 config.json（重启不丢）
- 手动 ask 全工具链简报 30s 完美（read_file→get_current_time→MEMORY→get_weather→cron_list→get_steps→LLM 简报）

## 2026-09-05~06（历史）

- **8 天白屏悬案终结**：UART 串口定位根因 = mediad 引用了固件 ffmpeg 未裁剪的 alsasink filter → 内核 panic → 白屏（与显示驱动无关）
- v11 修复（defconfig 3 行：ALSA_LIB/SPEEXDSP/ffmpeg filter 补 alsasink）验证通过：graph load succeed、mediad 全绿、屏幕正常
